#include <stdio.h>
#include "dog.h"

/**
 * main - check the code for alx students.
 * 
 * Return: Always 0.
 */
int main (void)
{
   struct dog my_dog;
	
   init_dog(&my_dog, "lenny", 3.5, "matty");
   printf(my name is %s, an i am %.if :) - woof!\n", my_dog.name, my_dog.age);
   return (0);
}
